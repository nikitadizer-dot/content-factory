# Mascots — AI-character reference library

_No mascots yet. Default mode per playbook: **varied-within-aesthetic** (new face each video, same aesthetic cluster) — do NOT lock an identity until a serial-character format proves itself._

## Aesthetic clusters to generate within (fashion vertical)
- streetwear founder (early 20s, urban)
- clean-girl POD seller (Etsy vibe)
- downtown designer (sketchbook energy)

## Workflow (when a recurring face is justified)
1. Generate persona via Higgsfield Soul (~$0.60), save `reference.jpg` + `description.md` in `{NN-slug}/`
2. Lock a `master.png` only for serial/anchor content
3. Index it here with: slug, aesthetic cluster, which idea/experiment it serves
